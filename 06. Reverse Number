#include <bits/stdc++.h> 
long long int reverseNumber(long long int n)
{
	//type your code here
	long long rev=0;
	while(n>0)
	{
		int lastdigit=n%10;
		rev=rev*10+lastdigit;
		n=n/10;
	}
	return rev;

}
